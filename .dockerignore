# Зависимости — ставятся внутри образа через npm ci
node_modules/

# Предыдущие сборки
dist/

# Локальные env-файлы — содержат localhost-адреса, в продакшн не нужны
.env
.env.local
.env.*.local

# Прочее
.git/
.gitignore
*.log
